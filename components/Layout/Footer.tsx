import * as React from "react";

interface IFooterProps {}

const Footer: React.FunctionComponent<IFooterProps> = (props) => {
  return <footer>Phoneword Converter App</footer>;
};

export default Footer;
